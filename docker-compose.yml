version: '3.8'

services:
  private-chat:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Mount a volume to persist the database file
      - chat-db-data:/app/server
    environment:
      - PORT=5000
    restart: unless-stopped

volumes:
  chat-db-data:
    driver: local

